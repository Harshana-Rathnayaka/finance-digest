import 'dart:ui';

abstract class AppColors {
  AppColors._();

  // text colors
  static const textGrey = Color(0xFF737373);
  static const hintColor = Color(0xFFA3A3A3);

  // button and background colors
  static const colorWhite = Color(0xFFFFFFFF);
  static const buttonColor = Color(0xFF523AE4);
  static const errorColor = Color(0xFFFF0000);
  static const colorBlack = Color(0xFF000000);
}